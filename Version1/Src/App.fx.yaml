App As appinfo:
    BackEnabled: =false
    OnStart: |-
        =Set(
            Legal,
            LookUp(
                Permisos,
                Persona.Email = User().Email,
                Título
            )
        );
        Set(
            N_C,true
        )
    Theme: =PowerAppsTheme

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

